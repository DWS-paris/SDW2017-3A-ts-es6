/*
Typer une variable
*/
    let userName: string = `Julien`
    let hobbies: Array<string | number> = [`CSS`, 34]
// 

/*
Typer une fonction
*/
    let twice = (attr: number): number => {
        return attr * 2
    }
    twice(10)
//